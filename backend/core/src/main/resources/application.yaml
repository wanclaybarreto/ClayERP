spring:
  application:
    name: ClayERP Core
  datasource:
    url: jdbc:postgresql://localhost:5432/clayerp-dev
    username: clayerp
    password: dev-123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080